/*
Given an integer array nums, sorted in ascending order (with distinct values) and a 
target value k. The array is rotated at some pivot point that is unknown. Find the 
index at which k is present and if k is not present return -1.

TC - O(logN)
SC - O(1)
*/

#include <bits/stdc++.h>

class Solution{
public:
    int searchBinary(std::vector<int> &arr, int target){
        
    }
};

int main(){
    return 0;
}